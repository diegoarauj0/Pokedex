import styled from "styled-components"

const Paragraph = styled.p`
    font-family: ${props => (props.theme.Righteous)};
    font-weight: 500;
    color: white;
    font-size: 18px;

`

export default {
    Paragraph
}